Tank {
  Program   = Block
  Block     = (Statement "\n"*)+
  Statement = "let" id ":" Type "=" value -- declAssign
            | "let" id ":" Type -- decl
            | "let" id "=" value -- declGuessType

  VarExp    = id
  Type      = "number" | "boolean" | "text"

  id        = ~keyword letter*
  keyword   = ("let" | "number" | "boolean" | "text")
  number    = digit+
  boolean   = ("true" | "false")
  value     = (number | id | boolean)
}
